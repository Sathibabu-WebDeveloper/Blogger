<%= simple_form_for(@question) do |f| %>
  <%= f.error_notification %>
   <%= render 'shared/errormsgs', object: f.object %>

  <div class="form-inputs">
    <%= f.input :question %>
    <%= f.input :featured %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
