<div class="panel panel-default">
        <div class="panel-body">
            <span>
                <h1 class="panel-title pull-left" style="font-size:30px;"><%= @user.first_name%> <%= @user.last_name%><small><%= @user.tag%></small> </h1>
                <div class="dropdown pull-right">
                   <!--  <button class="btn btn-success dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                     Follw
                    </button> -->
                    <%if current_user.present?%>
                     <%= render 'follow_form'%>
                     <%end%>
                </div>
            </span>
            <br><br>
           
            <br><br><hr>
            <span class="pull-left">

               
              
                <%= render 'stats'%>
                 <%#= link_to "Followers (#{@user.questions.length})", followers_user_path(@user) %>
                  <%#= link_to "Following (#{@user.questions.length})", following_user_path(@user) %>

               <!--  <a href="#" class="btn btn-link" style="text-decoration:none;"><i class="fa fa-fw fa-picture-o" aria-hidden="true"></i> Followers <span class="badge">42</span></a>
                <a href="#" class="btn btn-link" style="text-decoration:none;"><i class="fa fa-fw fa-users" aria-hidden="true"></i> Following <span class="badge">42</span></a> -->
            </span>
        </div>
    </div>